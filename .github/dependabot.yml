version: 2
updates:
  # Keep the SHA-pinned GitHub Actions fresh so pinning stays a security win,
  # not a stale-and-unpatched trap.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "ci"
